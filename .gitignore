.env
__pycache__/
*.pyc
*.pyo
.venv/

# Scenario templates are private (used in the separate test-harness project)
data/analysis/*/scenario_template.json

# Astro build output and dev cache
site/node_modules/
site/dist/
site/.astro/

# Site data is generated in CI from committed analysis files — not committed
data/site_data/

# attack_analysis.json and mitre_attack_cloud.json ARE committed
# (they form the persistent corpus; only scenario_template.json is excluded above)
.DS_Store
.envrc
