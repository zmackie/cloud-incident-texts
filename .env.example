# Copy this file to .env and fill in your values.
# The crawler works without these but vision OCR requires ANTHROPIC_API_KEY.

# Required for Claude vision OCR (PDF/image fallback)
ANTHROPIC_API_KEY=sk-ant-...

# Optional: raises GitHub API rate limit from 60 to 5000 req/hr
GITHUB_TOKEN=ghp_...
