.env
__pycache__/
*.pyc
*.pyo
.venv/

# Scenario templates are private (used in the separate test-harness project)
data/analysis/*/scenario_template.json

# Astro build output and node_modules
site/node_modules/
site/dist/
site/.astro/

# Site data is generated — don't commit the populated files
# (except the placeholder; generated files are built by CI)
data/site_data/*.json
data/site_data/incidents/
data/mitre_attack_cloud.json
data/analysis/
